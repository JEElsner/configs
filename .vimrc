" echo "Hello from .vimrc" "may not work by default?

" Relative numbering with absolute number on current line
set relativenumber
set number

" Set tabs the way I like: actually use tabs, and at a decent width
set tabstop=3
set shiftwidth=3
set expandtab
